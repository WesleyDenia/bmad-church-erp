# Styles

Esta pasta reserva estilos compartilhados que nao pertencem a uma pagina isolada.
Tokens globais continuam em `src/app/globals.css`; componentes devem preferir
`src/components/ui`, `src/components/design-system` e `src/components/operational`.
